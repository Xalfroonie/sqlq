-- MySQL Script generated by MySQL Workbench
-- Thu Apr 20 01:41:14 2023
-- Model: New Model    Version: 1.0
-- MySQL Workbench Forward Engineering

SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION';

-- -----------------------------------------------------
-- Schema mydb
-- -----------------------------------------------------

-- -----------------------------------------------------
-- Schema mydb
-- -----------------------------------------------------
CREATE SCHEMA IF NOT EXISTS `mydb` DEFAULT CHARACTER SET utf8 ;
USE `mydb` ;

-- -----------------------------------------------------
-- Table `mydb`.`Impuestos`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `mydb`.`Impuestos` (
  `FechaDeImpuesto` DATETIME NULL COMMENT 'Esta columna representa la fecha en la que se realizó el pago de impuestos correspondiente. Es importante tener un registro detallado de esta información para llevar un control y seguimiento adecuado de las obligaciones tributarias de la empresa. La fecha de impuesto puede ser utilizada para verificar el cumplimiento de los plazos establecidos por la autoridad tributaria y evitar posibles sanciones o multas.\n',
  `TipoDeImpuesto` TEXT NULL COMMENT 'En esta columna se registra el tipo de impuesto que se está pagando, ya sea IVA, ISR u otros impuestos que la empresa esté obligada a pagar. Es importante especificar claramente el tipo de impuesto para llevar un registro adecuado y poder realizar los cálculos fiscales correspondientes de forma precisa. Además, la información contenida en esta columna es fundamental para la elaboración de reportes y declaraciones fiscales.\n',
  `MontoDeImpuesto` DECIMAL(18) NULL COMMENT 'En esta columna se registra el monto pagado por la empresa en concepto de impuestos. Es importante llevar un control detallado de los montos pagados para poder hacer una correcta planeación fiscal y evitar posibles problemas con la autoridad tributaria. La información contenida en esta columna puede ser utilizada para verificar la exactitud de los cálculos fiscales y para la elaboración de reportes y declaraciones fiscales.',
  `DescripcionDeImpuesto` TEXT NULL COMMENT 'En esta columna se registra una breve descripción del impuesto que se está pagando. Esta descripción puede incluir información adicional sobre el impuesto, como su base legal, la tasa aplicable, entre otros. La información contenida en esta columna es de gran utilidad para entender de manera clara y precisa la naturaleza del impuesto pagado.\n',
  `Contador` TEXT NULL COMMENT 'En esta columna se registra el nombre del contador responsable del pago de impuestos. Es importante llevar un registro detallado del contador responsable para llevar un control adecuado y evitar posibles problemas en el futuro. Además, la información contenida en esta columna puede ser utilizada para la asignación de responsabilidades y para la elaboración de reportes y declaraciones fiscales.',
  `idImpuesto` VARCHAR(45) NULL,
  `Impuestoscol` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`Impuestoscol`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `mydb`.`Planillas`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `mydb`.`Planillas` (
  `SueldoBruto` INT NULL COMMENT 'Esta columna muestra el salario base que un empleado gana antes de deducciones o impuestos. Es importante tener un registro claro de los sueldos brutos de cada empleado para garantizar que se les pague de manera justa y acorde a su trabajo y responsabilidades.',
  `Descuentos` INT NULL COMMENT 'Esta columna registra todas las deducciones que se aplican al sueldo bruto de un empleado, como impuestos, seguros y otros gastos asociados al trabajo. Es importante tener un registro preciso de los descuentos para que tanto el empleador como el empleado sepan exactamente cuánto dinero se está deduciendo del salario bruto.\n',
  `Bonificacion` INT NULL COMMENT 'Esta columna registra cualquier pago adicional que un empleado pueda recibir además de su salario base. Esto podría incluir bonos por desempeño, comisiones o cualquier otra forma de pago adicional. Es importante tener un registro claro de las bonificaciones para garantizar que se paguen de manera justa y acorde a los logros y objetivos alcanzados por el empleado.\n\n',
  `SueldoNeto` DECIMAL(18) NULL COMMENT 'Esta columna muestra el salario total que un empleado recibe después de deducciones y bonificaciones. Es importante tener un registro preciso del salario neto para garantizar que se pague la cantidad correcta a cada empleado y para que los empleados puedan hacer un seguimiento de sus ingresos.',
  `HorasTrabajadas` NCHAR NULL COMMENT 'Esta columna registra el número de horas que un empleado ha trabajado en un período de tiempo determinado. Es importante tener un registro preciso de las horas trabajadas para garantizar que los empleados reciban la cantidad correcta de pago por su trabajo.',
  `DiasTrabajados` NCHAR NULL COMMENT 'Esta columna registra el número de días que un empleado ha trabajado en un período de tiempo determinado. Es importante tener un registro claro de los días trabajados para garantizar que los empleados reciban la cantidad correcta de pago por su trabajo y para mantener un seguimiento preciso de los registros de asistencia.',
  `Vacaciones` NCHAR NULL COMMENT 'Esta columna registra el tiempo de vacaciones que un empleado ha tomado en un período de tiempo determinado. Es importante tener un registro preciso de las vacaciones para garantizar que los empleados reciban la cantidad correcta de días de vacaciones y para mantener un seguimiento preciso de los registros de tiempo libre.',
  `IdPlanilla` VARCHAR(45) NOT NULL,
  `Vendedor` TEXT NULL,
  `Impuestos_Impuestoscol` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`IdPlanilla`),
  INDEX `fk_Planillas_Impuestos1_idx` (`Impuestos_Impuestoscol` ASC) VISIBLE,
  CONSTRAINT `fk_Planillas_Impuestos1`
    FOREIGN KEY (`Impuestos_Impuestoscol`)
    REFERENCES `mydb`.`Impuestos` (`Impuestoscol`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `mydb`.`Facturas`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `mydb`.`Facturas` (
  `Cliente` VARCHAR(45) NULL COMMENT 'Este campo registra el nombre del cliente que realizó la compra y que está asociado con la factura. El cliente puede ser una persona o una empresa que ha comprado uno o más productos o servicios del almacén. Este campo es importante para rastrear las compras de un cliente específico y para fines de contabilidad.',
  `NdeFactura` INT NOT NULL COMMENT ' Este campo registra el número de identificación único de la factura. Cada factura debe tener un número único para facilitar su seguimiento y para evitar errores en el registro. El número de factura también puede incluir información adicional, como el año y el mes en que se emitió la factura, para ayudar a identificarla con mayor precisión.',
  `FechaDeFactura` DATETIME NULL COMMENT 'Este campo registra la fecha en que se emitió la factura. La fecha de la factura es importante para llevar un registro preciso de las ventas del almacén y para fines de contabilidad. Además, puede ayudar a identificar patrones de ventas en función del tiempo, lo que puede ser útil para la toma de decisiones.\n',
  `Vendedor` VARCHAR(45) NULL COMMENT 'Este campo registra el nombre del vendedor que realizó la venta asociada a la factura. Este campo es importante para rastrear las ventas de cada vendedor y para fines de contabilidad. También puede ayudar a identificar a los vendedores que tienen un mejor desempeño y ofrecerles capacitación adicional para mejorar su rendimiento.',
  `DescripcionFactura` TEXT NULL COMMENT 'Este campo registra una descripción detallada de la factura, que puede incluir información sobre los productos o servicios comprados, los precios, los descuentos aplicados y otros detalles relevantes. Esta descripción es importante para tener un registro detallado de cada transacción y para ayudar a solucionar cualquier problema que pueda surgir.',
  `CantidadProductos` NCHAR NULL COMMENT 'Este campo registra la cantidad de cada producto o servicio comprado en la factura. Este campo es importante para llevar un registro preciso de las ventas y para ayudar en el control de inventario.',
  `PrecioUnitario` DECIMAL(18) NULL COMMENT ' Este campo registra el precio unitario de cada producto o servicio comprado en la factura. Este campo es importante para calcular el total de la venta y para llevar un registro preciso de las ventas.\n',
  `Impuestos` DECIMAL(18) NULL COMMENT 'Este campo registra los impuestos asociados a la venta, que pueden incluir impuestos sobre las ventas, impuestos sobre el valor agregado u otros impuestos aplicables. Este campo es importante para fines fiscales y contables.\n',
  `TotalDeFactura` DECIMAL(18) NULL COMMENT 'Este campo registra el total de la venta, incluyendo impuestos y descuentos. Este campo es importante para llevar un registro preciso de las ventas y para fines de contabilidad. También es útil para ayudar a los clientes a entender el costo total de su compra.',
  `Impuestos_Impuestoscol` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`NdeFactura`),
  INDEX `fk_Facturas_Impuestos1_idx` (`Impuestos_Impuestoscol` ASC) VISIBLE,
  CONSTRAINT `fk_Facturas_Impuestos1`
    FOREIGN KEY (`Impuestos_Impuestoscol`)
    REFERENCES `mydb`.`Impuestos` (`Impuestoscol`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `mydb`.`Cuentas`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `mydb`.`Cuentas` (
  `NdeCuenta` VARCHAR(45) NOT NULL COMMENT 'esta columna almacena un identificador único para cada cuenta en el sistema. Es esencial para rastrear y organizar todas las transacciones que ocurren en la cuenta. El número de cuenta puede estar compuesto por letras y números, y no puede repetirse en la misma tabla.\n',
  `Destinatario` VARCHAR(45) NULL COMMENT 'esta columna indica el destinatario de la transacción, que puede ser el nombre de un cliente, un proveedor, una cuenta bancaria, entre otros. La información almacenada aquí permite conocer quién recibió el dinero o el pago, lo que es especialmente útil para hacer un seguimiento del historial de compras y pagos.\n',
  `SaldoAbono` INT NULL COMMENT 'esta columna almacena el monto de dinero que se va a agregar a la cuenta en la transacción. Puede ser un número positivo o negativo, dependiendo del tipo de transacción que se esté realizando. Por ejemplo, si se está haciendo un pago, el valor de esta columna será negativo.',
  `FechaDeTransaccion` DATETIME NULL COMMENT 'esta columna almacena la fecha en que se realizó la transacción. Es una de las columnas más importantes para la contabilidad, ya que permite llevar un registro de todas las transacciones en el tiempo. La fecha se puede almacenar en varios formatos, pero se recomienda utilizar el formato YYYY-MM-DD.\n',
  `TipoDeTransaccion` TEXT NULL COMMENT 'esta columna indica si la transacción es un débito o un crédito en la cuenta. Un débito es cuando se retira dinero de la cuenta, mientras que un crédito es cuando se agrega dinero a la cuenta. Esta información es esencial para llevar un registro preciso del saldo de la cuenta.\n',
  `Descripcion` TEXT NULL COMMENT 'esta columna almacena una breve explicación de la transacción, que puede incluir detalles como el número de factura, el producto o servicio comprado, entre otros. Es importante ser detallado y preciso en la descripción, ya que ayuda a identificar cada transacción de forma clara y concisa.',
  `MontoDeTransaccion` DECIMAL(18) NULL COMMENT 'esta columna indica el valor monetario de la transacción. Puede ser un número positivo o negativo, y es esencial para llevar un registro preciso del saldo de la cuenta.',
  `SaldoActual` DECIMAL(18) NULL COMMENT 'esta columna indica el saldo actual de la cuenta después de la transacción. Es esencial para mantener un registro actualizado y preciso de la situación financiera de la cuenta.',
  `EstadoDeCuenta` TEXT NULL COMMENT 'esta columna indica si la cuenta está activa o inactiva, o si se han detectado problemas con la cuenta, como saldos negativos o pagos atrasados. La información almacenada aquí es esencial para tomar decisiones financieras importantes, como si se deben o no realizar más transacciones en la cuenta.',
  `Planillas_IdPlanilla` VARCHAR(45) NOT NULL,
  `Facturas_NdeFactura` INT NOT NULL,
  PRIMARY KEY (`NdeCuenta`),
  INDEX `fk_Cuentas_Planillas1_idx` (`Planillas_IdPlanilla` ASC) VISIBLE,
  INDEX `fk_Cuentas_Facturas1_idx` (`Facturas_NdeFactura` ASC) VISIBLE,
  CONSTRAINT `fk_Cuentas_Planillas1`
    FOREIGN KEY (`Planillas_IdPlanilla`)
    REFERENCES `mydb`.`Planillas` (`IdPlanilla`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_Cuentas_Facturas1`
    FOREIGN KEY (`Facturas_NdeFactura`)
    REFERENCES `mydb`.`Facturas` (`NdeFactura`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `mydb`.`Cajas`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `mydb`.`Cajas` (
  `NdeCaja` VARCHAR(45) NOT NULL COMMENT ' Este campo registra el número de la caja en la que se realizó la compra. Es importante llevar un registro del número de caja en el que se realizó cada venta para poder identificar de manera precisa las transacciones en la base de datos. De esta manera, se puede tener un seguimiento de las ventas por caja y se puede identificar si alguna de las cajas presenta problemas o necesidades de mantenimiento.',
  `Vendedor` VARCHAR(45) NULL COMMENT 'Este campo registra el nombre del vendedor que realizó la venta. Esta información es útil para identificar el rendimiento de cada vendedor y llevar un control de las ventas realizadas por cada uno. Además, esta información también puede ser utilizada para identificar problemas de atención al cliente o situaciones que puedan afectar la calidad del servicio, como por ejemplo, la falta de conocimiento del personal de ventas sobre los productos.\n',
  `montoDeCompra` INT NULL COMMENT 'Este campo registra el monto total de la compra. Esta información es esencial para llevar un control de las ventas y para la contabilidad de la empresa. Con la información de este campo se pueden generar reportes y estadísticas sobre las ventas y el rendimiento de la empresa. Además, también es útil para identificar los productos más vendidos y los productos con mayor rentabilidad.\n',
  `FechaDeCompra` INT NULL COMMENT 'Este campo registra la fecha en que se realizó la compra. Es importante llevar un registro detallado de la fecha en que se realizó cada venta para poder identificar las tendencias y patrones de compra. Esta información puede ser utilizada para identificar días o temporadas de mayor venta, lo que permite una mejor planificación de la gestión de inventario y una optimización del rendimiento del negocio.\n',
  `idProducto` VARCHAR(45) NULL COMMENT 'Este campo registra el identificador único del producto que se vendió. Es fundamental llevar un registro detallado de los productos vendidos para poder realizar un seguimiento preciso del inventario. De esta manera, se puede mantener un control adecuado sobre el stock de productos y se pueden identificar los productos más vendidos, lo que permite una mejor planificación de las compras y una optimización del rendimiento del negocio.\n',
  `Descuento` DECIMAL(18) NULL COMMENT 'Este campo registra el descuento aplicado a la compra (si es que hubo alguno). Es importante llevar un registro detallado de los descuentos aplicados para poder identificar su efectividad y su impacto en las ventas. Esta información también puede ser utilizada para identificar patrones de compra y para mejorar la planificación de las promociones y descuentos futuros.\n',
  `MetodoDePago` TEXT NULL COMMENT 'Este campo registra el método de pago utilizado para realizar la compra. Esta información es útil para llevar un control adecuado del flujo de efectivo y para la contabilidad de la empresa. Además, también es importante llevar un registro detallado de los métodos de pago utilizados para poder identificar los métodos de pago más populares entre los clientes y para poder adaptarse a las necesidades de los clientes.\n',
  `NdeCliente` TEXT NULL COMMENT 'Este campo registra el número de identificación del cliente que realizó la compra. Es importante llevar un registro detallado de los clientes para poder llevar un seguimiento adecuado de las ventas y para poder mejorar la atención al cliente. Esta información también puede ser utilizada para identificar patrones de compra y para mejorar la planificación',
  `Cuentas_NdeCuenta` VARCHAR(45) NOT NULL,
  `Facturas_NdeFactura` INT NOT NULL,
  PRIMARY KEY (`NdeCaja`),
  INDEX `fk_Cajas_Cuentas_idx` (`Cuentas_NdeCuenta` ASC) VISIBLE,
  INDEX `fk_Cajas_Facturas1_idx` (`Facturas_NdeFactura` ASC) VISIBLE,
  CONSTRAINT `fk_Cajas_Cuentas`
    FOREIGN KEY (`Cuentas_NdeCuenta`)
    REFERENCES `mydb`.`Cuentas` (`NdeCuenta`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_Cajas_Facturas1`
    FOREIGN KEY (`Facturas_NdeFactura`)
    REFERENCES `mydb`.`Facturas` (`NdeFactura`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;
